Python 3.11+ with Hatch build system and uv dependency management; FastAPI + Uvicorn + WebSockets for the web layer; Docker multi-stage builds with python:3.12-slim base; docker-compose for local orchestration of FastAPI, Celery worker, and Redis 7 Alpine; Playwright for headless UI smoke testing; ruff for linting/formatting and mypy for strict type checking under `pyproject.toml` overrides.