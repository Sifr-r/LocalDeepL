Python 3.11+ with Hatch build, uv dependency management, FastAPI + Uvicorn + WebSockets for the web layer, PyMuPDF for PDF rasterization/embedding, Surya for layout detection, OpenAI-compatible VLM endpoints (LM Studio/Ollama default), Celery + Redis for async translation, LangGraph for translation workflows, ChromaDB + sentence-transformers for optional lexicon RAG, and DocuVerse CSS for the single-page workstation UI.