# Audit Residual Open-Finding Sweep — 2026-08-16

**Method**: For each of the 21 findings in the executive summary, I checked the current state of the bug signature AND the fix signature in the working tree (`main` @ commit 6b4539d). Status = FIXED / PARTIAL / OPEN.

> Note: produced by the `explore` subagent (read-only). The `uv run mypy` proxy for finding #4 could not be run live; I did a static text-based check for the audit-fix pattern (`cast()` / `# type: ignore[no-any-return]`) in the two files.

## Summary

| Tier | Total | FIXED | PARTIAL | OPEN | Deferred |
| --- | --- | --- | --- | --- | --- |
| P0  | 2     | 0     | 1       | 1    | 0        |
| P1  | 5     | 0     | 1       | 4    | 0        |
| P2  | 9     | 0     | 2       | 5    | 2 (god-class, Svelte 4→5) |
| P3  | 5     | 0     | 0       | 5    | 0        |
| **All** | 21 | 0   | 4       | 15   | 2        |

## Per-finding table

| # | Sev | Finding | Location | Status | Evidence |
|---|-----|---------|----------|--------|----------|
| 1 | P0  | XXE in ingest_lexicon.py | scripts/ingest_lexicon.py:4, :42 | OPEN | `import xml.etree.ElementTree as ET` (line 4); `root = ET.fromstring(xml_content)` (line 42). The defusedxml-based `safe_xml_root` helper exists at src/omniscribe/core/glossary_sources/_common.py:147 and is wired in by tbx.py:31, xliff.py:28, tmx.py:29, but **ingest_lexicon.py never imports it** — it parses external GitHub-hosted TEI XML with stdlib `ET.fromstring`, which is XXE/DTD-unsafe. The 6ee5e5e commit's defusedxml migration covered only the glossary parsers. |
| 2 | P0  | Weak Redis password via VBScript Rnd() | start_app.vbs:78, :80; compose.yaml:86 | PARTIAL | Bug signature still present: `Randomize` (line 78) and `Int(Rnd() * Len(chars)) + 1` (line 80) inside `GetOrCreateRedisPassword()`. Fix signature partially present: `compose.yaml:86` now sets `--requirepass ${REDIS_PASSWORD:-...}` and `start_app.vbs:133` passes the generated password through to `docker run --requirepass`, but the **generator itself is still VBScript `Rnd()`** — a non-CSPRNG (LCG seeded by wall clock, no /dev/urandom or PowerShell `[System.Security.Cryptography.RandomNumberGenerator]` use). The password is now *used* correctly; it is still *generated* weakly. |
| 3 | P1  | Silent `except Exception` swallowing errors | src/omniscribe/core/ocr/processor.py:487, :566; src/omniscribe/core/pdf/embedder.py:121 | OPEN | All three sites still present and unchanged. `processor.py:487` — `except Exception: return ""` (pytesseract fallback, no logger). `processor.py:566` — `except Exception: return image_base64` (adaptive-threshold fallback, no logger). `embedder.py:121` — `except Exception: return True` (font probe, no logger). None narrowed to specific exception types, none use `logger.warning(...)`. |
| 4 | P1  | Type `Any` escapes in pyspellchecker / cv2 paths | src/omniscribe/core/postprocess.py:447; src/omniscribe/core/handwriting_preprocessor.py:74 | OPEN | Static check: `postprocess.py:443` `corrected = next(iter(candidates))` (from `spell.candidates(word)` line 433) flows to `corrected.upper()` at line 447 with no `cast(str, ...)` and no `# type: ignore[no-any-return]`. `handwriting_preprocessor.py:74` `img = cv2.cvtColor(np.asarray(pil), cv2.COLOR_RGB2BGR)` assigned to `img` (function declared `-> np.ndarray`) with no `cast()` and no ignore. The only `# type: ignore[no-any-return]` in `src/omniscribe/core/` lives in `glossary_sources/__init__.py:106` (different module). pyproject.toml:272 sets `warn_return_any = true`, so mypy would flag both. Live mypy was not run; text-based proxy above is the strongest static evidence available. |
| 5 | P1  | Missing ARIA associations in form primitives | frontend/src/lib/components/ui/Input.svelte; Select.svelte | PARTIAL | Bug signature still present: no `aria-describedby` or `aria-invalid` on either `<input>` (Input.svelte:43-72) or `<select>` (Select.svelte:31-57); the error/hint `<p>` is rendered below (Input.svelte:73-77, Select.svelte:67-71) but unlinked. Fix signature partially present: Select.svelte:16 has the `ariaLabel` prop (the 2bec3bf commit's fix for unlabeled comboboxes) and binds it at line 34. The audit-fix covered *labeling* but not *error/hint association*. |
| 6 | P1  | Tab ribbon missing WAI-ARIA tab roles | frontend/src/lib/components/ui/TabRibbon.svelte:77, :79 | OPEN | The container at line 77 is a plain `<nav class="...">` — no `role="tablist"`. Each tab button at lines 79-93 has no `role="tab"` and no `aria-selected={$activeTab === tab.tabKey}`. The `$activeTab === tab.tabKey` check is the only signal of state, and it drives only background color (line 86-89). A repo-wide grep for `role="tab` and `aria-selected` in `frontend/src/` returns **no matches**. |
| 7 | P1  | Broken multi-stage Docker build | Dockerfile:22 | OPEN | Single `FROM python:3.12-slim@sha256:... AS runtime-base` (line 22). There is no `AS builder` stage and no second `FROM ... AS runtime` for a slim final image. The `AS runtime-base` is just a stage *alias* on the only stage — the build still runs on the slim image itself, so the heavyweight `uv sync` of `transformers`/`torch`/`surya-ocr`/`chromadb` lands in the production image. The a77b77a commit hardened the image (digest pin, `UV_VERSION`, `uv-lock`, `.dockerignore`) but did not introduce multi-stage. |
| 8 | P2  | `HybridEngine` god-class (deferred) | src/omniscribe/core/workflows/hybrid.py | DEFERRED | File is **1168 lines**. Sizing only, no change proposed. |
| 9 | P2  | Duplicated `_parse_page_range_local` | src/omniscribe/core/pdf/rasterizer.py:188-215 | OPEN | Function still defined at lines 188-215 with the docstring "Locally scoped (not imported from `workflows.utils`) to break a circular-import chain: rasterizer -> workflows.utils -> workflows package init -> hybrid -> pdf". It mirrors the canonical `parse_page_range` in `core/workflows/utils.py` and is called at rasterizer.py:233 and :293. The duplication is documented as deliberate, not addressed. |
| 10 | P2  | Benign race on `_PYMUPDF_AGPL_NOTICE_EMITTED` | src/omniscribe/core/pdf/rasterizer.py:45 | OPEN | Flag is still a non-thread-safe module-level boolean at line 45. The docstring (lines 37-44) explicitly states the race is *documented rather than synchronised*. The `threading.Lock` (or `contextvars`) move the audit proposed has not happened. |
| 11 | P2  | WebSocket state not horizontally scalable | src/omniscribe/api/routers/websocket.py:189-499 | OPEN | `ConnectionManager.__init__` (lines 192-199) holds five in-process dicts: `active`, `_tokens`, `_minted`, `_cancel_flags`, `_accept_loops`. There is no Redis pub/sub, no external broker, no `asyncio.Queue` fan-out. The module docstring (lines 36-49) acknowledges "A restart … empties `active`, `_tokens`, and `_cancel_flags` in one shot". A grep for `pubsub|pub/sub|Channel\(|redis\.publish` in this file returns **no matches**. |
| 12 | P2  | Large WebSocket frame DoS | src/omniscribe/api/routers/websocket.py:612-622 | OPEN | The receive loop is `while True: raw = await websocket.receive_text()` (line 614) with **no** `max_size` argument, no manual length check, no middleware. A grep for `max_size|max-message-size|MAX_WS_MESSAGE` in the entire repo returns **no matches**. Starlette/FastAPI's default frame size is unbounded for receive on the WebSocket. |
| 13 | P2  | Legacy Svelte 4 patterns throughout (deferred) | frontend/src/**/*.svelte | DEFERRED | Pattern counts (excluding test fixtures under `__tests__/fixtures/`): **`$:` reactive statements in 20 files**, **`export let` in 11 files**, **`<slot>` in 7 files**. Roughly 20 source files need migration to Svelte 5 runes; UI primitives are the densest cluster. |
| 14 | P2  | Missing `aria-pressed` on toggle pills | frontend/src/lib/components/workstation/ProcessSettings.svelte:99-117 | OPEN | The document-processor pill buttons at lines 99-117 have **no** `aria-pressed={active}` attribute. A repo-wide grep for `aria-pressed` in `frontend/src/` returns **no matches**. The `<Toggle>` component (used at lines 125-155) is implemented as `role="switch"` + `aria-checked` (Toggle.svelte:35-36) which is the *switch* ARIA pattern, not the *toggle button* pattern — neither component satisfies the audit-fix the executive summary described. |
| 15 | P2  | Unpinned supply chain | install.ps1:27; .github/workflows/test.yml:124, :127, :143 | PARTIAL | **install.ps1**: line 27 still hits `https://astral.sh/uv/install.ps1` with no version pin. Gated by a file-size sanity check (lines 30-33) and a winget-first preference (lines 18-23). **.github/workflows/test.yml**: most actions are SHA-pinned with the `# v4` comment form, but the `container-scan` job still uses mutable tag refs: `docker/setup-buildx-action@v3` (line 124), `docker/build-push-action@v6` (line 127), `aquasecurity/trivy-action@0.28.0` (line 143). |
| 16 | P2  | Scripts excluded from Ruff | pyproject.toml:208 | OPEN | `extend-exclude` at lines 205-209 still includes `"scripts/**"` (line 208). The d125884 commit did not touch the ruff config. |
| 17 | P3  | Unused `type: ignore` in trocr_engine.py | src/omniscribe/core/trocr_engine.py:67 | OPEN (cannot confirm staleness without live mypy) | The `# type: ignore` is still on the same line. It is **not annotated with a specific error code** (no `[no-any-return]` / `[arg-type]` / `[call-overload]`), so even if the underlying error went away mypy would not warn about an unused ignore unless the project enables `warn_unused_ignores = true` (pyproject.toml:268 — *is* set, so mypy *would* catch a stale untyped ignore). Without a live mypy run the text-based check is inconclusive. |
| 18 | P3  | Extraction router `except Exception` mapping | src/omniscribe/api/routers/extraction.py:72, :121, :135 | OPEN | Three `except Exception` blocks: line 72 in `_load_tree_from_artifact`; line 121 in `export_blocktree`; line 135 in `extract_data`. None narrowed. The 6ee5e5e commit added extraction/translation router *test suites* but did not narrow these except blocks. |
| 19 | P3  | `Toggle.svelte` local mutation anti-pattern | frontend/src/lib/components/ui/Toggle.svelte:39 | OPEN | Line 39: `on:click={() => !disabled && (checked = !checked)}` — directly mutates the exported `checked` prop (line 15) inside the component. No `createEventDispatcher`, no `dispatch('change')`. |
| 20 | P3  | Brittle `try…finally` state patching in tests | tests/test_api_safety.py:356, :394, :473, :527 | OPEN (PARTIAL) | Manual `original_X = state.X; try: ... finally: state.X = original_X` patterns are still in use: text artifact restore at lines 356 / 394 / 473; export store restore at line 527. `monkeypatch.setattr` is used *elsewhere* in the suite and even within test_api_safety.py itself for env vars (line 258), but the `state.text_artifacts` / `state.metadata_artifacts` / `state.export_artifacts` writes specifically still use try/finally. |
| 21 | P3  | No enforced `--cov-fail-under` threshold in CI | .github/workflows/test.yml:73 | OPEN | Line 73: `uv run pytest -m "not slow" --cov=src/omniscribe --cov-report=term-missing --cov-report=xml -v` — coverage is *reported* (term + XML for `codecov`/`coveralls` ingestion) but no `--cov-fail-under=N` threshold is enforced. |

## Findings the report mis-cited or that turned out to be something else

- **Finding #5 (Input.svelte)**: the executive summary describes Input.svelte ~L66. The actual file has only 78 lines; the line the summary pointed at (66) is part of the focus-ring class string in the `<input class="...">` (line 66). The error/hint rendering block is at lines 73-77 (well below the cited line). The audit-fix signature (`aria-describedby` + `aria-invalid`) is missing on the *element*, not on the cited line; the line-cite is just imprecise.
- **Finding #6 (TabRibbon.svelte)**: the executive summary says ~L72. The actual file has 146 lines; line 72 is inside the v2.5 version-badge span (line 71-73). The actual `nav` and `button` elements the finding targets are at lines 77 and 79-93. Same pattern: line-cite is approximate, target is correct.
- **Finding #8 (HybridEngine line count)**: deferred, but the file is **1168 lines** — a 2x shrinkage from a full god-class would still leave ~500 lines per half.
- **Finding #9 (duplicated `_parse_page_range_local`)**: still present, but the docstring (rasterizer.py:191-195) explicitly says the local copy is *intentional* to break a `rasterizer -> workflows.utils -> workflows -> hybrid -> pdf` import cycle. Removing it is non-trivial — the cycle would have to be broken differently (e.g. moving `parse_page_range` into a leaf module that both `rasterizer` and `workflows.utils` can import from). Reporting as OPEN, not as "deferred by design".
- **Finding #10 (PyMuPDF race)**: same status. The docstring at rasterizer.py:37-44 is the project's explicit acknowledgement that the race is benign. The audit-fix (a `threading.Lock`) is a one-liner; it's open work, not a deferred design call.
- **Finding #14 (toggle pills)**: there are *two* different "toggle" UI patterns in this codebase. The `<Toggle>` component uses `role="switch"` (the correct ARIA pattern for a binary switch), and the document-processor chips in ProcessSettings.svelte are plain `<button>`s. The audit-fix (`aria-pressed`) is appropriate for the chips but **not** for the switch — applying it to the switch would be wrong. The right fix is per-element-type.
- **Finding #17 (trocr `type: ignore`)**: I could not run mypy, so I cannot confirm whether the untyped `# type: ignore` on trocr_engine.py:67 is stale. pyproject.toml:268 sets `warn_unused_ignores = true` so mypy *would* catch a stale untyped ignore if the underlying error had been fixed. The text-based check is inconclusive; the finding is OPEN-pending-mypy.
- **Finding #2 (Redis password)**: the P0 cite says "line ~1043" of start_app.vbs. The actual file is **much shorter than 1043 lines** — the `Randomize` / `Rnd()` live at lines 78 and 80, and the docker-run-with-`--requirepass` is at line 133. The audit-fix *is* present (requirepass set, pinned image, loopback bind), but the P0 finding the executive summary describes was about the *generator*, not the *consumer*. The cite is approximate; the bug signature is real and unchanged.
- **Finding #15 line numbers (install.ps1 ~L848)**: the file is only 152 lines; the unversioned URL is at line 27, the winget branch at line 18-23. Same approximate-cite pattern.

## Truly-open findings (action list)

Listed by severity. The action list is just the OPEN + PARTIAL ones.

**P0 (1 OPEN, 1 PARTIAL)**
- **#1 OPEN** — Replace `ET.fromstring(xml_content)` in `scripts/ingest_lexicon.py:42` with the existing `safe_xml_root` helper from `core/glossary_sources/_common.py:147` (or inline a `defusedxml.ElementTree.fromstring` call). One import change + one function call.
- **#2 PARTIAL** — The `--requirepass` consumer-side fix is in place; the remaining open half is the password *generator*. In `start_app.vbs:75-81`, replace the `Randomize` + `Rnd()` loop with a call out to a PowerShell one-liner that uses `[System.Security.Cryptography.RandomNumberGenerator]::Create().GetBytes(24)` and writes the result back to `redis-password.txt`. About 10 lines of VBS, no Python change.

**P1 (4 OPEN, 1 PARTIAL)**
- **#3 OPEN** — Three sites, same shape. At each `except Exception: return X` (processor.py:487, :566, embedder.py:121), narrow to the specific exception type and emit `logger.warning("OCR fallback path X failed: %s", exc, exc_info=True)` before returning the safe default. Embedder.py:121 is inside a font-probe try/finally — a narrow `except (RuntimeError, ValueError)` is appropriate.
- **#4 OPEN** — Two sites. At postprocess.py:447 wrap `corrected.upper()` in `cast(str, corrected.upper())` (and same for `:449`). At handwriting_preprocessor.py:74 wrap `cv2.cvtColor(...)` in `cast(np.ndarray, cv2.cvtColor(...))` (or `# type: ignore[no-any-return]` if the cast is too verbose). Requires running mypy once to confirm clean.
- **#5 PARTIAL** — Add `aria-describedby={error ? `${id}-error` : (hint ? `${id}-hint` : undefined)}` and `aria-invalid={error ? 'true' : undefined}` to both `<input>` (Input.svelte:43-72) and `<select>` (Select.svelte:31-57), and add matching `id` attributes to the helper `<p>` elements. The Select.svelte `ariaLabel` prop is already done; the rest is mechanical.
- **#6 OPEN** — In TabRibbon.svelte:77 change `<nav>` to `<nav role="tablist" aria-label="Primary">`. On each tab button (lines 79-93) add `role="tab"`, `aria-selected={$activeTab === tab.tabKey}`, and `aria-controls={...}` if the corresponding `role="tabpanel"` exists downstream. ~3 attribute additions.
- **#7 OPEN** — Add a builder stage to `Dockerfile`: `FROM python:3.12-slim AS builder` doing the `uv sync`, then a runtime stage `FROM python:3.12-slim` that `COPY --from=builder /app/.venv /app/.venv` and runs as `app`. This drops the heavy build chain from the final image.

**P2 (5 OPEN, 2 PARTIAL, 2 deferred)**
- **#9 OPEN** — Move `parse_page_range` into a leaf module (e.g. `omniscribe/core/pdf/page_range.py`) and import it from both `rasterizer` and `workflows/utils.py`. Docstring-acknowledged cycle breaks once both sides can reach the leaf without crossing the hybrid package init.
- **#10 OPEN** — One-liner: wrap `_emit_pymupdf_agpl_notice` in `with _AGPL_NOTICE_LOCK: ...` where `_AGPL_NOTICE_LOCK = threading.Lock()`. The docstring already explains the lock and notes "the cost is negligible".
- **#11 OPEN** — Multi-instance stateful WebSocket fan-out is the largest P2 item. Either a Redis-pub/sub adapter for `ConnectionManager` (with a per-process asyncio task that subscribes and re-broadcasts to its local sockets) or move progress emission to SSE. Out of scope for a one-line fix; needs a design pass.
- **#12 OPEN** — Add a per-message size cap on the WebSocket receive loop. Smallest fix: in the `while True` block at websocket.py:612-622, swap `await websocket.receive_text()` for a small helper that rejects frames larger than (say) 64 KiB, returning a 1009 (message-too-big) close. Alternatively pass `max_size=64 * 1024` to the receive call (Starlette `WebSocket.receive_text` does not currently accept it directly; a length check is the portable path).
- **#14 OPEN** — Add `aria-pressed={active}` to the document-processor pill buttons at ProcessSettings.svelte:99-117. (Do **not** add it to the `<Toggle>` switch — the switch uses `role="switch" aria-checked`, which is correct.)
- **#15 PARTIAL** — `install.ps1:27` should pin a specific uv version. `test.yml:124, 127, 143` should be SHA-pinned like the other actions in the file.
- **#16 OPEN** — Remove `"scripts/**"` from `extend-exclude` in `pyproject.toml:208` and fix whatever lint failures surface.
- **#8, #13** — DEFERRED per user. Sizing only.

**P3 (5 OPEN)**
- **#17 OPEN-pending-mypy** — Run mypy on trocr_engine.py and either delete the ignore or annotate the specific error code. Cheap if mypy is already green; otherwise needs the underlying typing to be fixed.
- **#18 OPEN** — Narrow the three `except Exception` blocks in extraction.py:72, :121, :135 to specific types. Line 72 in `_load_tree_from_artifact` should catch `(FileNotFoundError, KeyError, ValueError, json.JSONDecodeError)`. Line 121 in `export_blocktree` should catch the same shape. Line 135 in `extract_data` is a reasonable outer-net (after the `AIServiceError` is handled) and can stay broad, but a `logger.exception` is already there — the only change is narrowing the inner two.
- **#19 OPEN** — In Toggle.svelte, replace the on:click mutation (line 39) with a `createEventDispatcher` + `dispatch('change', !checked)`. Update the call sites in ProcessSettings.svelte (lines 129, 135, 141, 147, 154, 167) to use `on:change`.
- **#20 OPEN** — Convert the four `try: ... finally: state.X = original_X` blocks in test_api_safety.py (lines 356, 394, 473, 527) to `with monkeypatch.setattr(state, "X", replacement, raising=False):` (pattern already used in tests/test_health_endpoints.py:139-142). Test file change only; the assertion bodies are unchanged.
- **#21 OPEN** — Append `--cov-fail-under=70` (or whatever the team's chosen floor is) to the `pytest` invocation at .github/workflows/test.yml:73. One-flag change; pick a number that today's coverage comfortably exceeds.
