# Test Plan
Generated by /plan-eng-review (autoplan Phase 3, eng) on 2026-08-15
Branch: main
Repo: OmniScribe (d:\OmniScribe)
Feature: whitespace recall booster (plan docs/superpowers/plans/2026-08-14-whitespace-recall.md)
Full eng report: .autoplan/phase3-eng-report.md

## Verification already run (actual results, 2026-08-15)

| Command | Result |
|---|---|
| `uv run ruff check` (4 feature files) | All checks passed |
| `uv run ruff format --check` (7 files) | 7 files already formatted |
| `uv run mypy src` | Success: no issues found in 147 source files |
| `uv run pytest tests/test_text_recall.py tests/test_pipeline.py tests/test_workflows_hybrid.py -v` | 90 passed in 4.78s |
| `uv run pytest tests/test_aligner.py -v` (AGENTS.md gate for core/workflows/) | 36 passed in 4.16s |
| `uv run pytest tests/test_repo_hygiene.py -v` (install-parity pin) | 8 passed |
| `uv run pytest tests/test_pipeline_recall.py --collect-only` | 3 slow tests collected |

## Codepath → coverage diagram

```
[+] src/omniscribe/core/text_recall.py
  ├── WhitespaceRecallOptions.from_env
  │   ├── [★★★ TESTED] default enabled (unset)                 test_text_recall.py::TestWhitespaceRecallOptionsFromEnv::test_default_enabled
  │   ├── [★★★ TESTED] disable values (6 parametrized)         ::test_disable_values
  │   └── [★★★ TESTED] unrecognized stays enabled              ::test_unrecognized_value_stays_enabled
  └── WhitespaceRecallBooster.supplement
      ├── [★★★ TESTED] disabled → []                           ::test_disabled_options_return_no_boxes
      ├── [★★★ TESTED] cv2 missing → [] + no raise             ::test_missing_cv2_is_graceful
      ├── [GAP]        gray.size==0 (true 0x0 image)           blank-page test hits count<=1, NOT this branch
      ├── [★★  TESTED] count<=1 → [] (uniform page)            ::test_blank_page_yields_no_boxes
      ├── filter gates
      │   ├── [★★★ TESTED] px-height floor (rules)             ::test_rules_only_page_yields_no_boxes
      │   ├── [★★  TESTED] density/height via rules page       same test, gates not isolated
      │   ├── [GAP]        aspect-ratio gate alone (square blob)
      │   ├── [GAP]        max-area-fraction gate (>0.25 page)
      │   ├── [GAP]        max-density gate (solid blob >0.75)
      │   └── [GAP]        zero-height Surya boxes → min_height=0 (CEO G1)
      ├── dedup (_overlaps_surya)
      │   ├── [★★★ TESTED] containment branch                  ::test_candidate_inside_surya_box_is_dropped
      │   ├── [★★★ TESTED] IoU branch + no-intersection        ::test_overlaps_surya_iou_branch (lands AFTER CEO report; closes G2)
      │   └── [GAP]        multi-box straddle (each Surya box covers <50%) — T3
      ├── [GAP] per-page cap on survivors — T3
      └── [GAP] junk-class fixtures: gutter straddle / photo blob / dark background — T3 (CEO G6)

[+] src/omniscribe/core/workflows/hybrid.py (_apply_recall + _detect_layout)
  ├── [★★★ TESTED] merge + row-major re-sort                   test_workflows_hybrid.py::TestHybridWhitespaceRecall::test_detect_layout_merges_recall_boxes_and_resorts
  ├── [★★★ TESTED] booster=None byte-identical                 ::test_detect_layout_unchanged_without_booster
  ├── [★★  TESTED] booster exception → Surya boxes kept        ::test_booster_exception_keeps_surya_boxes (single page only)
  ├── [GAP]        multi-page partial failure (1 of 2 fails)   CEO G3
  ├── [GAP]        LRU miss → fallback decode + re-put         CEO G4 (hybrid.py:471-475)
  ├── [GAP]        disabled-options skip guard absent          T6 (hybrid.py:431, 466)
  └── [★★★ TESTED] wiring: env default/off                     test_pipeline.py::TestWhitespaceRecallWiring (2 tests)

[+] End-to-end
  ├── [GAP] [→E2E] recall box receives OCR text in DocumentResult (spec success criterion 2) — T4, fast tier
  └── [★★  TESTED] slow GT floor harness runs booster-on pipeline  tests/test_pipeline_recall.py (3 params, -m slow)
                   — measures pipeline recall floor, does NOT isolate booster on/off delta — T7 extends

COVERAGE: 12/26 paths tested  |  fast tier: 12/22 (55%)  |  GAPS: 14 (addressed by T3/T4/T6/T7 tasks + eng-new batch)
QUALITY: ★★★:8 ★★:4  |  no ★ smoke tests
```

Legend: ★★★ behavior + edge + error | ★★ happy path | ★ smoke check

## Proposed new tests (mapped to tasks)

| Task | Test(s) to add | File | Priority |
|---|---|---|---|
| T3 | gutter-straddle page (2 columns, tight gutter, line crossing it) → rejected/split; solid photo-like blob mid-density → rejected; dark/inverted background page → no junk explosion; per-page cap: 15 surviving candidates → ≤10 returned; zero-height Surya box in median input | tests/test_text_recall.py | P1 |
| T4 | E2E fast test: stubbed aligner misses one of three text lines, stub OCR returns crop text; assert the recall box carries text in `last_document_result`; same run with env OFF → byte-identical output | tests/test_text_recall_integration.py (new) or test_workflows_hybrid.py | P2 |
| T6 | `recall_booster` injected but `options.enabled=False` → `supplement` never invoked (spy); direct `_apply_recall` call with if-guard instead of assert | tests/test_workflows_hybrid.py | P3 |
| T7 | GT-backed recall harness: run booster ON vs OFF over examples/*.pdf with GT fixtures; assert recovered ≥ N on digital.pdf form-fill lines after retune; wire as `-m slow` | scripts/ + tests/test_pipeline_recall.py extension | P1 |
| T9 | edge test pinning the text-like-noise limitation (photo edges / figure borders yield boxes; documented as known limitation) | tests/test_text_recall.py | P3 (deferred per CEO D24) |
| eng-new | 0x0 image test for gray.size==0 branch; aspect/area/max-density isolated gate tests; multi-page partial failure; LRU fallback-decode path | tests/test_text_recall.py, tests/test_workflows_hybrid.py | P3 |

## Edge cases to verify manually (QA)

- 2-column PDF from examples/ with booster ON: no cross-column box in output (until T3 lands, EXPECT junk — this demonstrates H2).
- digital.pdf page 1 form-fill lines: after T7 retune, `Student Name …` blank-rule lines gain boxes.
- Same file ON vs OFF diff: OFF must be byte-identical (spec success criterion 3).
- Docker image: `python -c "import cv2"` succeeds; run summary (T2) appears at INFO once T2 lands.

## Eval suites that MUST run

1. Fast gate: `uv run ruff check src tests; uv run ruff format src tests --check; uv run mypy src; uv run pytest -m "not slow"`
2. Aligner gate (AGENTS.md, core/workflows/ touched): `uv run pytest tests/test_aligner.py -v`
3. Nightly slow tier: `uv run pytest -m slow` (includes tests/test_pipeline_recall.py GT floors — this is the closest existing thing to T7's harness)
4. Manual after T7 retune: `uv run python scripts/confidence_eval.py` ON vs OFF diff over examples/*.pdf

## Critical paths (must never regress)

- Booster OFF / cv2 missing / booster None → output byte-identical to pre-feature pipeline.
- Booster exception on any page → job completes with Surya boxes; never fails.
- Fast-tier tests stay independent of cv2 version drift (synthetic PIL fixtures only).
